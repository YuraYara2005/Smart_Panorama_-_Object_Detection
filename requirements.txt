# ─────────────────────────────────────────────────────────────
#  Smart Panorama & Object Recognition System — Requirements
#  Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# ── Core numerical / array computing ──
numpy>=1.24.0

# ── Computer Vision (headless = no GUI deps, safe for servers) ──
opencv-python-headless>=4.8.0

# ── Scientific computing (Median filter, signal processing) ──
scipy>=1.11.0

# ── Image quality metrics (SSIM) ──
scikit-image>=0.21.0

# ── Plotting & visualisation ──
matplotlib>=3.7.0

# ── Machine Learning (Classification module — Part 6) ──
scikit-learn>=1.3.0

# ── Dataset utilities / progress bars ──
tqdm>=4.65.0

# ── Notebook support (for notebooks/ directory) ──
jupyter>=1.0.0
ipykernel>=6.25.0
ipywidgets>=8.0.0

# ── Config file parsing ──
pyyaml>=6.0.1

# ── Image augmentation (optional, useful for dataset prep) ──
Pillow>=10.0.0